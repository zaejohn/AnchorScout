# Never put secret keys in committed files.

STELLAR_NETWORK=local
# STELLAR_RPC_URL=
# PUBLIC_STELLAR_NETWORK_PASSPHRASE=

# Public contract IDs may be stored here if your framework expects environment variables.
# PUBLIC_CONTRACT_ID=

# Server-only secrets belong in your local .env / deployment secret manager only.
